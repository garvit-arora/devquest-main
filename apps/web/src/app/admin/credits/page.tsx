import { AdminEmptyPage } from "../shared";

export default function AdminCreditsPage() {
  return <AdminEmptyPage title="Credits" copy="Inspect credit balances, ledger corrections, reward settlements, and platform budget limits." />;
}
