import { AdminEmptyPage } from "../shared";

export default function AdminModelsPage() {
  return <AdminEmptyPage title="Model aliases" copy="Manage DevQuest aliases, Azure deployment mappings, kill switches, and model availability." />;
}
