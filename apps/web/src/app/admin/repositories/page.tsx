import { AdminEmptyPage } from "../shared";

export default function AdminRepositoriesPage() {
  return <AdminEmptyPage title="Repository campaigns" copy="Add, pause, end, and review real repository campaigns after admin persistence is connected." />;
}
